name: Initialise Wiki

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - '.github/workflows/init-wiki.yml'

permissions:
  contents: write

jobs:
  init-wiki:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Push wiki Home page
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          WIKI_DIR="$(mktemp -d)"
          git clone "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.wiki.git" "$WIKI_DIR" 2>/dev/null || true
          cd "$WIKI_DIR"
          [ -d .git ] || { git init -b master; git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.wiki.git"; }
          cat > Home.md << 'WIKIEOF'
# NeuralMimicry Marketing — Wiki Home — Wiki Home

Brand assets, messaging frameworks, media plans, and video-production resources for the NeuralMimicry project. This repository is the working home for all marketing materials grounded in the live NeuralMimicry product suite.

> ☕ [Support NeuralMimicry on Crowdfunder](https://www.crowdfunder.co.uk/p/qr/aWggxwPW?utm_campaign=sharemodal&utm_medium=referral&utm_source=shortlink)

---

## Quick navigation

| Page | Description |
|---|---|
| [Getting Started](Getting-Started) | Build, configure, and run |
| [Contributing](Contributing) | How to raise issues and submit pull requests |

---

## Contents

- Brand assets and style guide
- Reusable messaging by product and solution route
- Media planning templates
- Video production resources and transcripts



## Commercial website

The public-facing commercial site is at [neuralmimicry.ai](https://neuralmimicry.ai). The site source is maintained separately.

## Crowdfunder

☕ [Support NeuralMimicry on Crowdfunder](https://www.crowdfunder.co.uk/p/qr/aWggxwPW?utm_campaign=sharemodal&utm_medium=referral&utm_source=shortlink)

## Get involved

- 🐛 [Report a bug or request a feature](https://github.com/neuralmimicry/marketing/issues)
- 💬 [Join the discussion](https://github.com/neuralmimicry/marketing/discussions)
- 📧 Direct support from the founder: [info@neuralmimicry.ai](mailto:info@neuralmimicry.ai) · **£1,000/day + VAT**
- 🌐 [neuralmimicry.ai](https://neuralmimicry.ai)
WIKIEOF
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git add Home.md
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "docs: initialise wiki Home page"
          git push -u origin HEAD:master 2>&1 || git push -u origin HEAD:main 2>&1
